{% extends 'base.html.twig' %}

{% block title %}Demande de reinitialisation mot de passe{% endblock %}

{% block body %}

    <section>
        <h1>Demande de reinitialisation mot de passe</h1>
        {{ form_start(passForm) }}
            {{ form_row(passForm.password) }}
            <button type="submit">Envoye</button>
        {{ form_end(passForm) }}
    </section>

{% endblock %}
